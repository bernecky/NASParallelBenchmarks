ModuleDec Cpx :

own:
{
implicit types:
  
explicit types:
  cpx = double[2];

global objects:

functions:
  cpx +(cpx a, cpx b);
  cpx *(cpx a, cpx b);
  cpx[+] + (cpx[+] a, cpx[+] b);
  int[+] shp(cpx[+] a);
}
